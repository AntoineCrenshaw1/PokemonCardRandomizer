"use client";
import React from "react";

const OpenPack = () => {
  return (
    <div>
      <button onClick={() => console.log("Pack opened")}></button>
    </div>
  );
};

export default OpenPack;
